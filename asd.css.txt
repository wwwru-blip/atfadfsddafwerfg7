* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: linear-gradient(135deg, #1a1a1a, #ff0000, #8fbc8f, #ffffff);
    background-size: 400% 400%;
    animation: gradientShift 15s ease infinite;
    color: #ffffff;
    overflow-x: hidden;
}

@keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

.container {
    max-width: 1200px;
    margin: auto;
    padding: 20px;
}

.header {
    text-align: center;
    padding: 40px 20px;
}

.main-title {
    font-size: 3em;
    font-weight: bold;
    margin-bottom: 20px;
}

.subtitle {
    font-size: 1.5em;
}

.timer-badge {
    background: red;
    padding: 10px 20px;
    border-radius: 30px;
    margin-top: 20px;
    display: inline-block;
}

.content-section {
    background: rgba(255,255,255,0.1);
    padding: 40px;
    border-radius: 20px;
    margin-top: 40px;
}

.cash-display {
    text-align: center;
}

.cash-amount {
    font-size: 4em;
    font-weight: bold;
}

.bonus-text {
    font-size: 1.5em;
    margin-top: 20px;
}

.cash-images {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    margin-top: 40px;
}

.cash-image-card {
    background: #2a5298;
    padding: 20px;
    border-radius: 15px;
    text-align: center;
}

.cash-image-placeholder {
    height: 150px;
    background: #00ab6b;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2em;
    border-radius: 10px;
    margin-bottom: 10px;
}

.cash-label {
    font-weight: bold;
}

.features {
    margin-top: 40px;
}

.feature-card {
    background: rgba(255,255,255,0.1);
    padding: 20px;
    border-radius: 10px;
    text-align: center;
}

.claim-section {
    text-align: center;
    margin-top: 40px;
}

.claim-button {
    background: limegreen;
    color: black;
    padding: 15px 40px;
    border-radius: 40px;
    font-weight: bold;
    text-decoration: none;
    display: inline-block;
    margin-top: 20px;
}

.footer {
    text-align: center;
    margin-top: 60px;
    padding: 20px;
    border-top: 1px solid rgba(255,255,255,0.3);
}
